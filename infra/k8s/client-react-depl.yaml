apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-react
spec:
  replicas: 2
  selector:
    matchLabels:
      app: client-react
  template:
    metadata:
      labels:
        app: client-react
    spec:
      containers:
      - name: client-react
        image: adhiana46/ms-node-client-react
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        # ports:
        # - containerPort: <Port>
